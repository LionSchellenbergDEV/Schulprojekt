<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Suchergebnisse</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style-searchResults.css">
</head>
<body>
<header id="header">
    <strong>Schulprojekt</strong>
    <nav>
        <a href="/">Startseite</a>
        <a href="/internal/tournament">Turnier</a>
        <a href="/auth/profile">Profil</a>
        <a href="/auth/logout">Abmelden</a>
    </nav>
</header>
<main>
    <form action="/auth/search" method="GET">
        <input type="text" name="username" placeholder="Nutzer suchen..." required><button type="submit">Suchen</button>
    </form>
<h3>Suchergebnisse</h3>

<% if (users.length > 0) { %>
        <% users.forEach(user => { %>
            <div class="card" style="width: 18rem;">
                <img src="/media/<%= user.character%>.png" class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title"><%= user.username %></h5>
                    <p class="card-text"><%= user.character%></p>
                    <a href="#" class="btn btn-primary">Turnier starten</a>
                </div>
            </div>
        <% }) %>
<% } else { %>
    <p>Kein Nutzer gefunden.</p>
<% } %>
</main>
</body>
</html>
